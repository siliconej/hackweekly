#include "llvm/IR/IRBuilder.h"

using namespace llvm;

Value* binaryOp(Value* lhs, Value* rhs) {
  return nullptr;
}

int main(int argc, char** argv) {
  return (binaryOp(nullptr, nullptr) != nullptr);
}
